[build-system]
requires = ["setuptools", "versioneer[toml]"]
build-backend = "setuptools.build_meta"

[tool.versioneer]
VCS = "git"
style = "pep440"
versionfile_source = "nimare_gpu/_version.py"
versionfile_build = "nimare_gpu/_version.py"
tag_prefix = "v"
parentdir_prefix = ""